//import Schedule from './schedule.service';
import Collection from './collection.service';

//create new object
const api = () => ({
  //Schedule: () => ({ ...Schedule }),
  Collection: () => ({ ...Collection })
});
export default api;